# Excel計算過程出力とプレビュー

実装日: 2026-09-17。元仕様は [sdc-excel-calculation-report-spec.md](sdc-excel-calculation-report-spec.md)。

## 利用方法

GUIの「Excelをプレビュー」で、メモリ上の帳票を「計算過程Excel」タブに表示する。
シート選択、表示位置、倍率、縦横スクロール、識別列固定、セルの数式・完全な値の表示、
内部リンクから明細への移動ができる。入力・出力設定を変えると表示は無効になる。
NDUモデル図とKG編集は「入力・保存設定とモデル」タブに残す。

「変換して保存」はモデル、JSON、既定オンのExcelを同一実行IDで保存する。
チェックを外した場合も帳票プレビューは使用でき、xlsxの保存だけを省略する。
保存済みExcelは独立したボタンで関連付けアプリから開ける。開く操作の失敗は保存結果を取り消さない。

開発環境では `uv pip install --python .venv/Scripts/python.exe -r requirements.txt` を実行し、
従来のVS Code F5構成、または `scripts/sdc_converter_app.py` から起動する。
配布EXEにはXlsxWriterを同梱し、利用PCでExcel・Node.js・Pythonの追加インストールは不要。

4つの個別CLIに `--excel-report path.xlsx` を追加した。
未指定時は従来のCLIを通す。モデル出力オプションを省略するとExcelだけの計算確認になり、
帳票に「計算確認・モデル未保存」と表示する。水平のmidpoint/skip、土圧のendpoints/左押し、
丸め桁数等を維持する。周面でモデルを書き出す場合のprofile明示も維持する。

## 構成

- `calculation_record.py`: 計算に使用したパーサの戻り値、入力スナップショット、最終NDUから
  出典、幾何、実入力846欄、変更前後、除外、支点・ケース構造を記録。表示文字列を再解析しない。
- `excel_report.py`: 6シートの共通セルモデルと限定数式AST。四則演算、MAX/MIN/SUM/ROUNDを
  同じASTからExcel式・計算済みキャッシュに変換する。最終値をDecimalの実入力と照合し、
  不一致時は対象とセル・式を示して失敗する。xlsx公開前にZIP/XMLを読戻して全式・数値・空欄を照合する。
- `excel_preview.py`: 上記セルモデルをTk Canvasに表示する読取専用プレビュー。
  出力Excelとシート・セル位置・数式・値・色を共用する。外部で編集された任意のxlsxを読み込む機能ではない。
- `portable_converter.py`: 既存のDecimal計算は維持し、共通記録・帳票・保存を統合する。
- `excel_cli.py`: Excel指定時の個別CLIの記録と保存。共通のstage/publish_new/verify_sourcesを使用。

SDC原値参照のキーは「処理＋SDC行＋CSV欄」。同値の1列目と3列目も別セルを参照する。
日本語シート参照は引用符付き。先端の負側制限値は実際の空セル。
数値の原十進文字列とDecimalの丸め前文字列も残す。実入力は固定値で、Excel編集からNDUへ戻さない。

座標の二進引算誤差が丸め境界を変えないよう、座標演算に限って入力座標・層境界・条件の
最大十進精度＋中点の1桁を保持するROUNDを用いる。元精度を切り捨てず、微小値の加算はしない。
補間土圧・台形積分・加重和は途中で丸めず、既存ルールの最終桁だけを丸める。

水平地盤ばね・有効抵抗土圧以外の印刷はA4横、列幅から用紙の有効幅へ収まる倍率を算出し、縦は必要ページ数とする。
列幅の基準となるNormal書式はテーマに依存しないCalibri 11に固定し、日本語Excelでも横幅を維持する。表示セルは游ゴシックを使用する。
複数種類の表があるため、改ページごとにその表の見出しを再掲する。
ExcelのFitToPagesは手動改ページを無効にするため併用しない。
見出し挿入に伴うセル位置変更は、数式AST・内部リンク・検証セルへ一括反映する。

## 水平地盤ばねの見本形式（2026-09-17改訂）

承認済みの [修正プラン](horizontal-spring-excel-layout-plan.md) に沿い、
`test/今町橋りょう4P(C方向･右押し→)_変換済み.ndu.計算過程.見本xlsx.xlsx` の水平シートを再現する。

- 選択したSDC列順に7列の表を横配置。層番号・層厚・原値Kh・部材全長・重なり長・部材番号・計算Khを表示。
- 部材と地層の重なりを1行とし、複数層の内訳を展開する。データの縦結合は行わず、層境界と部材境界に応じた罫線でまとめる。
- 見本入力ではA1:U34、3ブロック×24部材、各30区間、33行目にΣ。見出しの結合はD2:E2、K2:L2、R2:S2のみ。
- 主表はMS 明朝10pt、白地、長さ小数3桁、Kh整数表示。Khセル内は原値参照または丸め前の加重平均式とし、表示の丸めと実際の採用値を区別する。
- 「入力根拠」末尾に水平用の地層・部材幾何・照合・正確な計算記録を追加。丸め後の採用再計算値と固定実入力を照合し、不一致は差セルに表示する。
- 変換結果と照合表から、水平主表の該当部材へ内部リンクで移動できる。原値参照はSDC行・CSV欄ごとに独立している。
- `midpoint` は中央の層の原値、`skip` は保持値または空欄を表示し、方式を表題に付記する。既存の変換計算・JSONスキーマは変更しない。
- 印刷はA4縦・90%、左右0.7in・上下0.75in、7列ごとの縦改ページ、1～2行目の見出し反復。見本入力は3ページ。長い表は部材境界で改ページし、1ページに収まらない内訳には部材番号と「続き」を表示する。
- GUIも可変列数・見出し結合・罫線・配置を共有する。水平では固定列を解除し、表示位置から各KGブロックへ横移動できる。数式バーは表示丸め前の値を示す。

水平改訂時の検証は全130テスト成功。72部材の原値・加重平均をFractionによる独立計算と照合し、
1/2/4ブロック、異なる分割数、4層跨ぎ、小数原値と0.5丸め、中央境界、保留の空欄・0、70層の改ページ、
GUIのU列・結合見出し選択・横方向の内部リンクを確認した。
Microsoft Excel 16.0で全4,351数式・348最終照合値の一致、原値変更時の対象列だけの再計算、固定実入力の保持を確認。
水平表のPDFは3ページで、文字切れ・罫線・Σまで目視確認した。変換NDUは改訂前の出力とバイト一致。
土圧・周面・先端シートは実行日時を除くセル値・式・書式・改ページが改訂前と一致した。

検証ファイルは無視対象の `build/horizontal-spring-implementation/` に保存する。
入力SDC/NDUと指定の `test` 見本は変更していない。別の `snap` 見本には作業中の外部変更があり、そのまま保持した。

## 有効抵抗土圧の見本形式（2026-09-17改訂）

承認済みの [修正プラン](effective-pressure-excel-layout-plan.md) に沿い、指定の `test` 見本の土圧シートを再現する。
名称と単位はSDCを正とし、見本の誤記「土圧力度 (kN/m2)」を「土圧力 (kN/m)」へ修正する。数値の換算は行わない。

- 選択KGをモデル列順に9列ずつ横配置する。右押しではSDC総列数を基準に土圧列を反転し、見本入力はKG4/5/6にSDC3/2/1列目が対応する。
- 区間の上下端を2行で表示し、部材全長・部材番号を縦結合する。複数層では区間長も2行結合し、採用土圧は部材の最初と最後へ置く。
- 見本入力は3ブロック×24部材・30区間、A1:AA64（印刷はA1:AA63）、180結合、63行目Σ。主表はMS 明朝10pt、白地、長さ小数3桁、土圧は既定小数1桁の表示。セルの丸め前値を保持する。
- 層上下の原値を使った線形補間と台形積分をExcel式で表し、層途中の区間でも元の層上端から補間する。単一層・積分平均・上下端採用・跨ぎ拒否と、0～6桁の最終丸めは従来どおり。
- 「入力根拠」の水平補足の後に、土圧の地層・部材幾何・区間計算・144端点の採用照合・Decimal丸め前文字列を追加した。採用用ROUNDと固定実入力を区別し、差を表示する。
- 水平と土圧の同時選択時は「変換結果」のA列に水平リンク、K列に土圧リンクを置く。土圧のみなら従来のA列リンクを用いる。
- 印刷はA4縦100%、左右18mm・上下19mm、9列ごとの縦改ページ、1～2行反復、既存フッターを維持。見本入力は41行目前の部材境界で改ページし、各KG2ページ、計6ページになる。
- 長い部材はページごとに結合を分割し、部材番号と「続き」を表示する。部材全長は最初の値だけをΣに集計する。ブロック数や部材・層数は固定しない。
- GUIは同じ矩形結合と上下寄せを描画する。結合の途中のクリックでも数式・値を持つ先頭セルを選択し、先頭行が画面外でも結合の可視範囲を描く。表示位置から9列単位で各KGへ移動できる。
- xlsx保存前に結合範囲の重複と覆われるセルの値を検査し、保存後のXMLで結合・数式・数値・空欄を照合する。数値0と数式キャッシュを持つ結合セルも保存する。

土圧改訂後は全140テスト成功。全72部材144端点のFraction独立計算、原値変更、左押し、非既定方式・丸め桁、
1/2/4ブロック、異なる分割、層途中の区間、4層以上跨ぎ、0値、70層・長い部材の改ページ、GUIと内部リンクを確認した。
Microsoft Excel 16.0で全4,876数式の再計算値とキャッシュを照合し、最大差は約2.91×10⁻¹¹。全348採用照合値は固定実入力と一致した。
原値編集時のKG4だけの再計算と固定実入力の保持を確認し、土圧6ページ・水平3ページのPDFを全ページ目視確認した。
NDU出力予定バイト列は改訂前と一致し、水平・周面・先端のセル値・式・書式・改ページも実行日時を除いて一致した。

確認用ブックは [有効抵抗土圧修正版.xlsx](../../outputs/effective-pressure-review-20260917-223857/今町橋りょう4P_計算過程_有効抵抗土圧修正版.xlsx)。全4工程を含むモデル未保存の計算確認用。
検証記録は無視対象の `build/effective-pressure-implementation/` に保存した。入力SDC/NDUと指定見本は変更していない。

## 保存の境界

Excelを先に生成・検証し、モデル・JSON・Excelを保存先の一時ファイルへ書いて読戻し、
必要なバックアップを作成して帳票を公開し、入力を再検証して最後にモデルを公開する。
通常失敗では、この実行が公開し、内容とファイルIDが変わっていない帳票だけを取り消す。
作成したバックアップは保持してエラーにパスを付記する。Excel既存ファイルは再利用しない。
複数ファイルの公開は一括原子的ではなく、強制終了時には帳票だけが残る可能性がある。

## 検証

既存回帰に加え、全対象・区間数、最終846欄、Fractionによる独立積分、空欄6欄、
周面除外12節点、非連番先端・小数値、部分項目・部分杭列、非既定CLI設定、
原値変更で別杭列に影響しないこと、固定実入力の保持、Excelオン/オフ同一モデル、
生成失敗・Excel/JSON/モデル公開失敗・他プロセスによる帳票差替えの保持、GUI切替・無効化を検証する。

初回実装時はMicrosoft Excel 16.0の独立インスタンスで、全348最終再計算セルと全4,486数式セルを再計算し、
値・キャッシュの一致を確認した。SDC原値変更に応じた再計算、変換時実入力の固定保持も確認した。
実機検証は生成した検証用ブックだけを対象とし、入力原本と参考Excelには保存しない。
全6シートの代表範囲をExcel出力PDFから画像化して表示・数値書式・空欄を確認する。
Input-JR/JRSNAPでの解析実行、別クリーンPC、署名は今回の検証に含めない。
